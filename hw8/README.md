```
Bayan usage:
  -b [ --block-size ] arg (=2048) Block size for hash algorithm (Bytes)
  -d [ --dir ] arg                Directories
  -e [ --exclude ] arg            Exclude directories
  -H [ --hash-type ] arg (=MD5)   Hash algorithm: MD4, MD5, SHA1, SHA224, 
                                  SHA256, SHA384, SHA512, RIPEMD160
  -h [ --help ]                   Help
  -r [ --recursive ]              Recursive
  -s [ --size ] arg (=1)          File minimum size (Bytes)
  -t [ --time ]                   Show execution time
```
